# API Docs

This directory is generated from Go doc comments.

To refresh it:

```bash
make docs-godoc
```

The generated files are plain-text API references wrapped in Markdown so they can be browsed easily on GitHub.
